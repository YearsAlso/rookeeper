# rookeeper TODO

## 1. 文档目标

本文件基于 `docs/industrial-single-node-coordination-service-v1.0-prd.md` 制定，作为项目阶段性开发工作的执行清单，用于统一说明：

1. 当前版本分阶段要完成的开发工作。
1. 每个阶段应产出的成果物。
1. 每个阶段的验收标准。
1. V1.0 范围与后续分布式演进边界。

## 2. 版本目标与范围

### 2.1 V1.0 目标

交付一个面向工控单机场景的协调服务，满足以下核心目标：

1. 单机部署，不引入 Raft、Zab 等分布式一致性协议。
1. 提供树形 KV、Watcher、ACL、单机锁、服务注册发现、WAL 持久化与恢复。
1. 兼容 Linux 与 Windows，优先支持 x86_64，预留 ARM。
1. 满足轻量、确定性、可恢复、可审计的工业现场要求。

### 2.2 V1.0 范围

| 优先级 | 内容 | 是否纳入 V1.0 |
| --- | --- | --- |
| P0 | 树形 KV/元数据、WAL/快照、Watcher、ACL、单机锁、同机服务发现、Rust SDK、CLI、平台适配 | 是 |
| P1 | 热冷分层基础能力、回放工具、本地 TCP 备用传输、基础指标、OPC UA/MQTT 插件预留、更完善认证 | 尽量纳入 |
| P2 | 多节点复制、选主、mTLS、跨主机发现、多租户、远程归档 | 否 |

## 3. 开发阶段总览

| 阶段 | 目标 | 主要内容 | 关键成果 | 阶段结论 |
| --- | --- | --- | --- | --- |
| Phase 0 | 项目基线搭建 | 架构、工程骨架、跨平台构建、协议与数据模型定稿 | Cargo workspace、架构说明、协议草案、CI 基线 | 为功能开发提供统一底座 |
| Phase 1 | MVP 核心存储闭环 | 树形 KV、WAL、恢复、ACL、基础 CLI | 可启动的本地协调内核 | 实现最小可用本地状态机 |
| Phase 2 | 事件与协调能力完善 | 持久 Watcher、锁、服务注册发现、SDK 基础接口 | 多进程协调可用 | 满足工业单机协调主场景 |
| Phase 3 | 平台与运维能力完善 | Windows/Linux IPC、服务集成、日志、指标、审计、回放 | 工程化交付能力 | 达到现场部署条件 |
| Phase 4 | V1.0 收敛与发布 | 性能压测、恢复验证、兼容性验证、文档与发布包 | V1.0 候选版本 | 可正式对外发布 |
| Backlog | 分布式演进预研 | 复制日志、远程传输、会话扩展 | 设计预研材料 | 不进入 V1.0 开发主线 |

## 4. 分阶段 TODO

## Phase 0：项目基线搭建

### 开发工作

1. 建立 Rust 工程目录与 Cargo workspace 结构，至少拆分 `server`、`client`、`protocol`、`storage`、`cli`、`platform` 模块。
1. 定义全局架构边界：状态机层、存储层、传输层、会话层、Watcher 分发层。
1. 定义树形命名空间、节点元数据、ACL、命令日志、事件模型的数据结构。
1. 定义长度前缀二进制协议基础格式，包括协议头、请求类型、错误码、版本字段。
1. 确定运行时与基础依赖选型，优先 `tokio`、跨平台文件锁、结构化日志、序列化与校验组件。
1. 建立跨平台构建与测试基线，至少覆盖 Linux 和 Windows。
1. 明确单实例保护机制、数据目录布局、默认配置文件格式。

### 生成成果

1. Cargo workspace 与基础 crate 骨架。
1. `docs` 下的架构说明、协议草案、存储布局说明。
1. CI 配置：Linux/Windows 编译通过。
1. 统一错误码枚举与配置模板。

### 验收标准

1. 项目可以在 Linux 和 Windows 上完成基础编译。
1. 核心模块职责边界清晰，无平台特有逻辑泄露到业务层。
1. 协议、数据模型、错误码已固化到文档和代码接口中。
1. 数据目录结构和配置项可供后续实现直接复用。

## Phase 1：MVP 核心存储闭环

### 开发工作

1. 实现树形 KV 核心数据模型，支持 `create/get/set/delete/ls`。
2. 实现路径规范化，兼容输入中的 `\` 和 `/`，内部统一为 Unix 风格。
3. 实现版本控制、基础节点元数据、持久节点与临时节点语义。
4. 实现 ACL 基础能力：读、写、创建、删除、管理。
5. 实现确定性状态机执行器，所有写操作通过命令驱动。
6. 实现 WAL 持久化，保证写入在持久化后对外可见。
7. 实现快照与恢复流程：快照加载 + WAL 重放。
8. 实现基础 CLI：`get`、`set`、`create`、`delete`、`ls`、`status`。
9. 实现最小单机服务进程启动流程和本地数据目录初始化。

### 生成成果

1. 可运行的单机服务原型。
2. 树形 KV 和 ACL 基础实现。
3. WAL、快照、恢复机制。
4. CLI 基本命令集。
5. 基础集成测试与恢复测试样例。

### 验收标准

1. 服务首次启动可完成数据目录初始化并提供本地读写能力。
2. 同一路径上的并发更新具备版本冲突检测能力。
3. 异常退出后可通过快照与 WAL 重放恢复到最后一致状态。
4. CLI 能完成最小的节点管理与状态查看。
5. 状态机重放对同一命令序列产生完全一致结果。

## Phase 2：事件与协调能力完善

### 开发工作

1. 实现持久 Watcher，支持路径级和前缀级订阅。
1. 实现事件顺序投递与事件结构定义，至少包含命令号、路径、事件类型、版本。
1. 实现 Watch 背压策略，避免慢消费者阻塞核心写路径。
1. 实现会话管理机制，支撑临时节点、连接生命周期和锁释放。
1. 实现单机锁能力：阻塞获取、非阻塞尝试、超时获取、异常释放。
1. 基于节点模型实现服务注册发现，支持实例注册、列举、订阅上下线变化。
1. 实现 Rust SDK 基础接口，覆盖连接、节点读写、Watch、锁、服务发现。
1. 为 CLI 增补 `watch`、`lock`、`acl` 命令。

### 生成成果

1. 持久 Watcher 子系统。
1. 单机锁实现与示例。
1. 服务注册发现实现与路径规范。
1. Rust SDK 初版。
1. 多进程协调示例程序。

### 验收标准

1. Watch 订阅在服务运行期间可持续生效，不是一次性消费。
1. 单个订阅内事件顺序与状态机提交顺序一致。
1. 多进程竞争同一把锁时，结果可预测且可观察。
1. 异常断开会话后，临时节点与关联锁能按规则释放。
1. SDK 可在 Linux 和 Windows 上以一致方式完成节点访问与事件订阅。

## Phase 3：平台与运维能力完善

### 开发工作

1. 完成 IPC 抽象层：Linux 优先 UDS，Windows 优先 Named Pipe，可选本地 TCP 备用实现。
1. 完成平台适配细节，包括文件锁、路径处理、进程身份/句柄差异封装。
1. 提供 Windows Service 集成能力。
1. 提供 Linux systemd 服务文件与部署说明。
1. 实现结构化 JSON 日志，统一会话号、命令号、路径、错误码字段。
1. 实现基础指标导出，包括延迟分布、WAL 大小、存储字节数、连接数、Watch 数量、恢复耗时。
1. 实现审计日志与录制/回放工具基础能力。
1. 为认证与未来 mTLS 预留接口，至少固化协议字段和认证边界。

### 生成成果

1. 跨平台 IPC 层与平台适配模块。
1. Windows Service 与 systemd 交付资产。
1. 结构化日志与指标输出。
1. 审计与回放工具初版。
1. 平台部署文档与运维手册。

### 验收标准

1. Linux 与 Windows 启停、连接、数据访问语义一致。
1. 默认日志可直接被机器解析，关键操作具备可审计字段。
1. 基础指标可被外部系统采集或通过本地接口查询。
1. 平台部署材料可支持服务安装、启动、停止和故障恢复。
1. 不启用可选插件时，核心资源占用仍满足轻量目标。

## Phase 4：V1.0 收敛与发布

### 开发工作

1. 完成性能与恢复专项验证，重点验证延迟抖动、启动恢复、内存占用和二进制大小。
1. 完成 Linux/Windows 双平台兼容性回归。
1. 补齐配置、错误码、数据目录、部署方式、CLI 使用说明。
1. 清理不稳定接口，固化对外 SDK 和协议版本。
1. 输出 V1.0 发布说明、升级说明和限制说明。
1. 对 P1 内容做取舍：可纳入基础指标、回放工具、本地 TCP 备用传输；其余保留到后续版本。

### 生成成果

1. V1.0 Release Candidate。
1. 完整用户文档、部署文档、运维手册。
1. 兼容性与性能测试报告。
1. 发布包与校验信息。

### 验收标准

1. 达到 PRD 中的核心非功能目标：P99 延迟抖动、恢复时间、二进制大小、空闲内存占用。
1. V1.0 范围内的 P0 能力全部闭环。
1. 已知限制、非目标与后续计划已明确记录。
1. 发布包可用于现场试点部署。

## 5. MVP 范围落地清单

MVP 以“先完成单机可用协调闭环”为目标，优先覆盖 PRD 中最小可行产品范围。

### MVP 必做

1. 本地树形 KV 存储。
1. 基础 Watcher。
1. ACL。
1. WAL 持久化与恢复。
1. 基础 Rust SDK。
1. 基础 CLI。
1. 单机锁基础能力。

### MVP 可暂缓

1. 高级过滤 Watcher。
1. OPC UA / MQTT 输出。
1. 热冷分层高级策略。
1. 完整回放工具链。
1. 更复杂认证机制。

### MVP 验收标准

1. 单进程与多进程场景下都能完成配置共享与状态通知。
1. 异常退出后可以恢复到最后一致状态。
1. 节点、ACL、Watch、锁四类基础能力可通过 CLI 和 SDK 使用。
1. Linux 与 Windows 的基本行为一致。

## 6. 模块级工作拆解

| 模块 | P0/P1/P2 | 当前目标 | 关键成果 | 验收重点 |
| --- | --- | --- | --- | --- |
| 核心存储引擎 | P0 | 树形状态机 + 版本语义 | 节点模型、命令执行器 | 原子写、版本冲突、可重放 |
| 持久化 | P0 | WAL + 快照 + 恢复 | 日志段、快照文件、恢复流程 | 一致恢复、损坏检测 |
| Watcher | P0 | 持久订阅 + 顺序投递 | 订阅表、事件总线 | 顺序性、背压隔离 |
| 锁 | P0 | 单机互斥协调 | 锁 API、等待机制 | 异常释放、可观测 |
| 服务发现 | P0 | 同机注册与发现 | `/services` 约定路径 | 列举、订阅、自动失效 |
| SDK | P0 | Rust 官方接入层 | client crate | 错误模型清晰、接口稳定 |
| CLI | P0 | 现场运维与调试入口 | 命令集、JSON 输出 | 易用、可脚本化 |
| 平台适配 | P0 | Linux/Windows 一致行为 | IPC 抽象、服务集成 | 双平台可运行 |
| 可观测性 | P1 | 日志、指标、审计 | JSON 日志、metrics | 低开销、可追踪 |
| 高级扩展 | P2 | 分布式化预留 | 协议扩展点、日志格式稳定 | 不影响 V1.0 主线 |

## 7. 非功能专项清单

### 确定性

1. 状态机执行不可依赖随机数或墙上时钟决定业务结果。
1. 回放同一命令日志必须得到相同状态与事件序列。

### 性能

1. 读写延迟抖动 P99 < 1ms。
1. 启动与恢复目标 < 500ms。
1. 服务二进制目标 < 10MB。
1. 空闲内存占用目标 < 20MB。

### 稳定性

1. 慢订阅者不应阻塞主写路径。
1. 客户端异常断开不应破坏服务可用性。
1. 日志滚动与快照过程不应导致状态损坏。

### 跨平台

1. Linux 与 Windows 的路径、ACL、错误码语义尽量保持一致。
1. 禁止依赖 `fork` 等 Linux 专属系统调用。
1. Named Pipe、UDS、本地 TCP 必须有统一抽象。

## 8. 当前执行顺序建议

1. 先完成 Phase 0，固定工程结构、协议、数据模型和配置布局。
1. 紧接着完成 Phase 1，做出可恢复的本地 KV 最小闭环。
1. 然后完成 Phase 2，把 Watch、锁、服务发现和 SDK 串起来。
1. 最后进入 Phase 3 和 Phase 4，补齐运维、平台化、性能与发布能力。

## 9. 分布式版本预留 Backlog

以下内容只做接口与设计预留，不进入 V1.0 开发承诺：

1. 复制日志格式与远程传输协议。
1. Leader/Follower 角色模型。
1. 成员管理与故障转移。
1. 跨节点 Watch 广播。
1. 跨节点服务注册发现。
1. mTLS 与证书体系。

## 10. 完成定义（Definition of Done）

某一阶段标记完成前，必须同时满足：

1. 阶段内约定的功能已经实现，并形成对应成果物。
1. 阶段验收标准全部可验证。
1. 对外文档、配置说明和已知限制已同步更新。
1. Linux 与 Windows 两个平台的基本行为已完成验证。
1. 不因新增能力破坏确定性、恢复性和轻量化目标。
